sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t timbissell/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true timbissell/react-test npm test

after_success:
  - docker build -t timbissell/multi-client ./client
  - docker build -t timbissell/multi-nginx ./nginx
  - docker build -t timbissell/multi-server ./server
  - docker build -t timbissell/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push timbissell/multi-client
  - docker push timbissell/multi-nginx
  - docker push timbissell/multi-server
  - docker push timbissell/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'us-east-1'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-us-east-1-923445559289'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
